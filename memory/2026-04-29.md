# 2026-04-29

## Task 5 Hermes skill import debrief

- Implemented `ask skill import --agent hermes` for adopting existing local Hermes skills into `ask.lock` without moving or rewriting skill directories.
- The import flow supports `--dry-run`, `--all`, named imports, and `--global`; it rejects omitted/mixed/non-Hermes agents and rejects combining `--all` with explicit names.
- Imported lock entries record conservative provenance: `agent: hermes`, `ownership: imported`, `install_mode: in-place`, `update_strategy: none`, `target_path`, and deterministic directory checksum.
- Lock handling now distinguishes agent-scoped entries and duplicate same-name Hermes skills by target path so ASK does not conflate unrelated agent skills.
- Scanner/list metadata matching was adjusted to ignore non-Hermes lock entries and to match imported nested skills by target path.
- Subagent implementation and review were used; parent follow-up fixed spec/quality issues around missing named imports, `--all` plus names, explicit and legacy non-Hermes lock collisions, and same-name target-path identity.
- A second review initially requested changes for legacy no-agent non-Hermes lock handling; fixed by treating legacy no-agent lock entries as Hermes only when their source/URL is a known Hermes source such as `hermes-index` or `NousResearch/hermes-agent/...`.

## Task 6 Hermes install provenance debrief

- Implemented Hermes-specific provenance for `ask skill install --agent hermes` lock entries while preserving the existing generic install lock entry for backward compatibility.
- Hermes install lock entries now record `agent: hermes`, `ownership: ask`, `install_mode: ask-cache`, `source_path`, `target_path`, deterministic `sha256:` checksum, and `update_strategy` (`hermes-index` when sourced from Hermes index metadata, otherwise `git`).
- Added additive lock fields `source_identifier` and `source_path`; repository candidates and GitHub repository DTOs now preserve Hermes index `source_identifier` and `update_strategy` through search/fetch/list-to-install conversion.
- Installer rejects bundled Hermes sources under `NousResearch/hermes-agent/skills`; official optional/user-installable Hermes sources remain manageable.
- Parent review found and fixed two quality gaps after the first implementation: Hermes index provenance could be lost outside manual metadata injection, and checksum failures were silently ignored. The fix propagates/infers source metadata for cached/configured Hermes paths and fails install if a Hermes checksum cannot be generated.
- Added tests for Hermes install provenance, cached official optional provenance inference, lock lookup preference for agent-specific entries, Hermes index metadata preservation, and checksum determinism/.git/symlink exclusion.
- Verification completed: targeted `go test ./internal/installer ./internal/config ./internal/repository ./cmd`, full `go test ./...`, independent re-review PASS, and fresh `/tmp/ask-task6-verify` manual smoke test using isolated `HOME`/`HERMES_HOME` all passed.

## Task 6.1 install prompt/config-scope fix debrief

- Investigated user report that `ask skill install` could hang on a GitHub username prompt when trying the Hermes smoke install.
- Root cause was twofold: install command config loading still used project-default `config.LoadConfig()` instead of command-scoped `loadConfigForCommand(cmd)`, so `--global` ignored `~/.ask/config.yaml`; unresolved `official/gitnexus-explorer` then fell through to GitHub shorthand `https://github.com/official/gitnexus-explorer`, where `git clone` prompted for credentials.
- Fixed install config loading to honor `--global` / explicit command scope through `loadInstallConfig(cmd)`, with a regression test covering global Hermes repo config.
- Added a shared git command helper that sets `GIT_TERMINAL_PROMPT=0` for clone/sparse-checkout/checkout paths so failed public/private clones return an error instead of blocking on interactive credential prompts.
- Verified with targeted tests, full `go test ./...`, and manual isolated smoke: `ask skill install gitnexus-explorer --repo hermes-index --agent hermes --global --skip-score` succeeds when global config contains the canonical Hermes index.

## Task 6.2 Hermes list bundled-copy filtering debrief

- Investigated user-supplied `/home/openclaw/dev/ask/live-list.json` from `ask skill list --agent hermes --global --json`; it contained 139 live Hermes user-skill entries, including 83 names/paths that overlap the bundled Hermes distribution under `~/.hermes/hermes-agent/skills`.
- Confirmed 76 of the overlapping user-skill directories were byte-identical regular-file copies of bundled/core distribution skills, so the list output was too noisy and looked like ASK might manage bundled/core skills.
- Added scanner filtering that omits only exact bundled distribution copies by comparing the installed skill's relative path and regular-file SHA-256 digests against the sibling bundled distribution directory (`../hermes-agent/skills`). Modified local copies at the same path remain visible.
- Added regression tests for omitting exact bundled copies and retaining modified local copies.
- Verification completed: targeted `go test ./internal/hermes ./cmd ./internal/installer ./internal/config ./internal/repository`, full `go test ./...`, live smoke with `env -u HERMES_HOME go run . skill list --agent hermes --global --json`, and independent review PASS. Live smoke reduced the real list from 139 entries to 63 by filtering 76 exact bundled copies.
- Subagent review noted non-blocking caveats: the filter intentionally ignores non-regular files/empty dirs, and an ASK/import-managed exact bundled duplicate would be filtered before lock metadata is applied; acceptable for this safety/noise fix.
## Task 7 Hermes update debrief

- Implemented Hermes-aware `ask skill update --agent hermes` and top-level `ask update --agent hermes` routing with `--force` support, leaving generic update behavior on the existing path.
- Added `internal/hermes.PlanUpdate` to select only ASK-owned Hermes lock entries with update strategies `hermes-index` or `git`; imported/in-place/local-only entries are skipped/refused as unavailable, bundled entries are refused, and named unsafe updates return errors rather than being silently skipped.
- Dirty-state protection now verifies deterministic `sha256:` checksums for the ASK source cache and any distinct non-symlink Hermes target copy; symlink targets rely on source verification. Local edits are refused unless `--force` is set.
- Update reinstalls use installer replacement mode with locked name/source/target expectations, suppress extra generic lock writes, and refuse replacement if upstream `SKILL.md` resolves to a different skill name or computed paths do not match the managed lock paths.
- Hermes-index update inputs trust two-segment source identifiers only when the lock entry source is `hermes`; nested Hermes index identifiers use validated `NousResearch/hermes-agent` URLs to avoid falling through to arbitrary GitHub shorthand.
- Review found and parent fixed blockers around checksum prefix normalization, stale lock/name mismatch risk, source-identifier trust boundary, replacement path safety, and dirty copied target coverage.
- Verification completed: targeted `go test ./internal/hermes ./cmd ./internal/installer -count=1`, full `go test ./...`, final blocker review PASS, and isolated manual smoke using temp `HOME`/`HERMES_HOME`: Hermes-index install succeeded, clean update succeeded, dirty source update refused, forced update succeeded, and dirty copied target update refused.

## Task 8 Hermes uninstall debrief

- Implemented Hermes-aware `ask skill uninstall --agent hermes` routing with conservative ownership/provenance checks before any file deletion.
- Added `internal/hermes.PlanUninstall` to classify uninstall actions from lock metadata: ASK-owned cache installs remove the Hermes target, ASK source cache, and tracking only when checksum/source-root safety checks pass; `--forget` is tracking-only; imported/in-place skills require explicit `--forget` or `--delete-files`; bundled and unmanaged/native Hermes skills are refused.
- Added Hermes uninstall CLI flags `--forget` and `--delete-files`, rejected non-basename/path-like Hermes skill names, rejected mixed Hermes/non-Hermes agent selections, and kept generic uninstall behavior separate.
- Added scoped lock removal helpers so Hermes uninstall removes the selected Hermes/legacy-Hermes lock entry without deleting unrelated generic or other-agent same-name entries.
- Review found and parent fixed blockers around destructive `--forget` semantics, path normalization of `../skill`, legacy no-agent Hermes lock compatibility, and overly broad lock-removal fallback.
- Verification completed: targeted `go test ./internal/hermes ./internal/config ./cmd -count=1`, full `go test ./...`, final blocker review PASS, and isolated manual smoke using temp `HOME`/`HERMES_HOME`: ASK-owned uninstall removed source/target/tracking, imported default uninstall refused with guidance, imported `--forget` preserved files, and imported `--delete-files` removed files/tracking.

## Task 9 Hermes source policy docs/config debrief

- Added `hermes-index` to generated default ASK repositories as the canonical `type: hermes` source for user-installable Hermes optional skills.
- Added config tests asserting the default repo set includes `hermes-index` with `https://hermes-agent.nousresearch.com/docs/api/skills-index.json` and does not include the bundled `NousResearch/hermes-agent/skills` tree.
- Updated English and Chinese docs (`README*`, `docs/commands*`, `docs/configuration*`, `docs/skill-sources*`) to clarify that ASK does not manage Hermes bundled/core skills, recommends `hermes-index`, and treats direct `optional-skills` as explicit advanced configuration.
- Documented Hermes-safe lifecycle behavior: update only ASK-owned/provenance-tracked skills; imported/in-place uninstall preserves files unless `--forget` or explicit `--delete-files`; bundled/core skills are refused.
- Cleaned live global ASK config `/home/openclaw/.ask/config.yaml` by removing only the unsafe `hermes-bundled` repo (`NousResearch/hermes-agent/skills`), preserving `hermes-index` and `hermes-optional`; backup saved at `/home/openclaw/.ask/config.yaml.task9-backup-20260429T121951Z`.
- Verification completed: targeted `go test ./internal/config -count=1`, full `go test ./...`, `git diff --check`, and independent review PASS.

## Task 10 Hermes lifecycle final review debrief

- Reviewed final uncommitted Task 10 lifecycle cleanup for Hermes import/install lock behavior.
- Verified the targeted test suite `go test ./cmd ./internal/hermes ./internal/config -count=1` passes after the changes.
- Review result: PASS; no blockers found for dry-run all-scan behavior or Hermes-only generic lock suppression.
- Final E2E smoke used isolated temp `HOME`/`HERMES_HOME` only; it verified default `hermes-index` source policy, local native import dry-run/import/list/uninstall-forget behavior, Hermes-index install provenance, safe update, dirty update refusal, and ASK-owned uninstall source/target/tracking cleanup.
- Task 10 found and fixed two final lifecycle gaps: bare Hermes import `--dry-run` now safely scans all without writing tracking, and Hermes-only installs suppress stale legacy generic lock entries so uninstall can remove all ASK-owned tracking.

## skills.sh source support debrief

- Added explicit opt-in `skills.sh` repository source support for ASK; no default config entry was added because live `/api/v1/skills*` endpoints currently require an API key despite docs suggesting unauthenticated lower-rate access.
- Implemented `skills.sh` as discovery/catalog metadata only: list/search/fetch use native ASK source dispatch and HTTP client code; no `npx skills add` or skills.sh special installer path was introduced.
- Added parser/resolver coverage for documented `/api/v1` payloads and legacy compact search payloads. GitHub-backed entries resolve to native ASK GitHub refs; domain/artifact/non-GitHub entries remain visible but are marked unsupported with clear reasons.
- Added `SKILLS_SH_API_KEY`/repo-token bearer auth, context-aware requests, a 30s HTTP timeout, 5 MiB response limit, focused 401/429/503 errors, and token redaction for response snippets.
- Wired search/install UX so unsupported skills.sh entries are shown in text/JSON output and are never passed to the installer; supported entries install through native refs while preserving `source_identifier` and `update_strategy: skills.sh` provenance.
- Final review found and fixed a blocker where mixed repo-name expansion could still queue unsupported entries. Verification completed: `git diff --check`, targeted `go test ./cmd`, `go test ./internal/repository ./internal/config ./cmd -count=1`, full `go test ./...`, and final independent re-review PASS.
