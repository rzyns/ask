# 2026-04-28 Debrief

## Task 1: Hermes source classification helpers

- Added strict TDD coverage for Hermes source classification in `internal/hermes/skills_test.go`, first verifying the missing implementation failed to compile as expected.
- Implemented `internal/hermes/skills.go` with `HermesSourceKind`, `HermesSourceClassification`, and `ClassifyHermesSource`.
- Learned/encoded policy:
  - `NousResearch/hermes-agent/skills` and children are bundled Hermes sources and not ASK-manageable.
  - `NousResearch/hermes-agent/optional-skills` and children are official optional sources and ASK-manageable.
  - `https://hermes-agent.nousresearch.com/docs/api/skills-index.json` is the canonical manageable index source.
  - Unknown non-bundled sources are conservatively treated as manageable.
- Normalization handles whitespace/trailing slashes, `.git` suffixes, GitHub `tree`/`blob` browser URLs with arbitrary branch segments, and case-insensitive owner/repo matching.
- Verification passed:
  - `go test ./internal/hermes`
  - `go test ./internal/hermes ./internal/config ./internal/repository`
- Issues: none encountered.

## Task 2: Reject bundled Hermes repo sources

- Followed TDD by adding failing coverage before implementation:
  - `cmd/repo_hermes_test.go` for the repo-add helper rejecting bundled `NousResearch/hermes-agent/skills` and children while allowing `optional-skills`.
  - `internal/repository/source_test.go` and `fetch_test.go` for dir source search/fetch rejection of bundled Hermes sources.
  - `internal/repository/hermes_index_test.go` for skipping Hermes index entries resolving to bundled `skills` paths.
- Implemented minimal wiring using `internal/hermes.ClassifyHermesSource`:
  - `repo add` now rejects bundled Hermes skills before `validateSkillsRepo` network validation.
  - dir source search/fetch now return a clear unsupported-source error for bundled Hermes skills instead of calling GitHub/git discovery.
  - Hermes index candidate mapping skips bundled Hermes `skills` entries while preserving official optional mapping to `optional-skills`.
- Error text includes: `bundled Hermes skills` and `Use hermes-index for user-installable Hermes skills.`
- Verification passed:
  - `go test ./cmd ./internal/repository`
  - `go test ./cmd ./internal/repository ./internal/hermes`
- Issues: targeted failing tests initially attempted live GitHub/git work for bundled dir sources, confirming the missing pre-dispatch rejection; no unresolved issues.

## Task 3: Hermes installed-skill scanner

- Followed strict TDD by first adding `internal/hermes/installed_test.go` and verifying `go test ./internal/hermes` failed to compile because `ScanInstalledSkills`, `InstalledScanOptions`, and ownership types were missing.
- Implemented `internal/hermes/installed.go` with a read-only scanner that:
  - Recursively discovers directories containing `SKILL.md` up to default depth 5 or caller-provided `MaxDepth`.
  - Skips hidden/dot directories such as `.hub` and skips symlink directories without following them.
  - Uses existing `skill.FindSkillMD` and `skill.ParseSkillMD` for SKILL.md detection/parsing.
  - Returns forward-slash relative paths, absolute discovered paths, frontmatter description/version, and directory basename fallback when frontmatter name is absent.
  - Sorts deterministically by relative path, then name.
  - Treats unknown installed entries as `Ownership=hermes-native`, `Managed=false`, `Source=local`, `UpdateStrategy=none`.
  - Marks lockfile-matched entries as ASK-managed only when the lock name uniquely matches a top-level installed skill, copies lock source, falls back to lock version only when metadata version is empty, and uses `UpdateStrategy=git` only when lock URL is non-empty.
  - Returns nil/empty for missing or blank skills directories.
- Reused existing `config.LockFile`/`config.LockEntry` metadata for lockfile-backed ownership classification.
- Verification passed:
  - `go test ./internal/hermes ./internal/skill`
- Issues: none encountered.
## ASK Hermes lifecycle Task 4 - Hermes-aware list output
- Added Hermes-specific list path for `ask skill list --agent hermes`, routing through the Hermes installed-skill scanner instead of the generic one-level directory lister.
- Added additive list JSON fields: `managed_by`, `status`, `source`, and `update`; Hermes JSON items also include agent/scope/path.
- Human Hermes list output now includes ownership/status/source/update columns.
- Scanner/list errors are reported instead of silently converting malformed skills into empty results.
- Verification passed with targeted tests and full `go test ./...`; committed as Task 4 after review fixes.

